
-------第一周----
aws的ec2部署独立环境的sd,使用anconda------dreambooth的版本和webui不兼容的问题
aws的云部署(每个用户一个endpoint, 不能定制镜像)----不符合我们的要求
更新阿里云镜像，安装一些需要的插件和基础镜像



-------第二周----
google用gke部署sdwebui，中心思想是一个大模型部署一个独立的服务，底层是多个服务器。 所以多个大模型就需要很多台服务器 
优势是推理可以共享gpu
缺点是，如果大模型太多会需要比较多的节点资源
解决了多用户webui不出现进度条的问题
未解决：oom的问题，可能和镜像版本有关系（需要刷新浏览器解决）
       如果一个人修改了pod配置，还是会影响是用该pod的其他人


---- 后续(推理集群部署上线) ----
pod的弹性伸缩有些问题
打包固定版本的镜像,安装各个插件
初期采用固定的pod数量，固定时间弹缩
固定一个pod来进行调度管理
---- 训练集群-------
训练模型暂时还是用阿里云EC2，后续可考虑接入api自定义界面在gke进行训练